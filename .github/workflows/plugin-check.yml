name: Plugin Check
on: pull_request
permissions: {}
jobs:
  skip:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - run: echo "stubbed for H1 security research to avoid burning CI minutes"
